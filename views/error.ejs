<%- include('partials/header', { title: title || 'Error', user: null }) %>

<div class="row justify-content-center">
    <div class="col-md-6 text-center">
        <div class="error-page mt-5">
            <h1 class="display-1 text-danger mb-4"><%= title || '404' %></h1>
            <h2 class="mb-4"><%= message || 'Page not found' %></h2>
            <p class="lead mb-4">Sorry, the page you are looking for might have been removed or is temporarily unavailable.</p>
            <a href="/" class="btn btn-primary">
                <i class="fas fa-home me-2"></i>Return to Homepage
            </a>
        </div>
    </div>
</div>

<%- include('partials/footer') %>